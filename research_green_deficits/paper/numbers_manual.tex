% numbers_manual.tex -- hand-transcribed result macros for drivers the
% exporter does not yet cover. This file is NEVER regenerated, so re-running
% export_paper_numbers cannot wipe these values (which it did once, breaking
% the build). Each block cites the table it was read from.
%
% Every macro is also \providecommand-guarded in the preamble, so a missing
% value degrades to the "pending" marker rather than a compile failure.

% ---- Frictionless ownership economy (tables: two-asset ownership) ----
\newcommand{\OwnDlnPLS}{+0.007}
\newcommand{\OwnDlnPLevy}{+0.021}

% ---- Ownership + infrequent adjustment, BENCHMARK ----
% Full grid (n_b=60, n_k=34, n_x=150); payout phi=1; Stone-Geary shift
% bbar=0.03; discount factor recalibrated to 0.874 against the direct
% liquid-holding target.
\newcommand{\OwnKVLam}{0.15}
\newcommand{\OwnKVSb}{0.30}
\newcommand{\OwnKVTopTen}{66}
\newcommand{\OwnKVTopOne}{32}
\newcommand{\OwnKVW}{1.8}
\newcommand{\OwnKVPrem}{5.9}
\newcommand{\OwnKVDlnPLS}{-0.023}
\newcommand{\OwnKVDlnPLevy}{+0.014}
% Coarse-grid pass of the same specification (grid robustness). Latest rerun,
% after the experiment-bracket fix tightened the bisection tolerance.
\newcommand{\OwnKVDlnPLSf}{-0.011}
\newcommand{\OwnKVDlnPLevyf}{+0.012}
% Without the Stone-Geary shift (bbar=0), coarse grid.
\newcommand{\OwnKVDlnPLSb}{-0.012}
\newcommand{\OwnKVDlnPLevyb}{+0.032}
% Consumption threshold below which hand-to-mouth status is attainable.
\newcommand{\OwnKVcThresh}{1.06}

% ---- Friction-only limit (no convenience utility) ----
\newcommand{\KmvHtM}{54}
\newcommand{\KmvTopTen}{98}

% ---- Restoration at the KVJ point estimate (table: ..._kv_z10) ----
% zeta = 1 with chi rescaled by (b_target + bbar)^(zeta-2) = x3.03 so the
% marginal value of liquidity at the calibrated position is unchanged
% (curvature-only rotation). beta recalibrated to the direct-holding target.
\newcommand{\ZtenChi}{0.0068}
\newcommand{\ZtenBeta}{0.866}
\newcommand{\ZtenLS}{-0.059}
\newcommand{\ZtenLevy}{+0.056}
\newcommand{\ZtenW}{3.7}
\newcommand{\ZtenTopOne}{67}
\newcommand{\ZtenOmega}{0.081}
\newcommand{\ZtenQ}{3.41}
\newcommand{\ZtenYield}{3.5}
\newcommand{\ZtenHtM}{29}

% ---- Two-instrument wealth calibration, M3 (table: ..._kv_w30) ----
% beta -> W, chi_b -> omega. Converged to the liquid-share FLOOR, not to the
% target: chi fell 0.00223 -> 0.00012 (factor 19) while omega moved only
% 0.133 -> 0.135. Sign contrast survives at W = 2.68; magnitudes not
% reported (chi ~ 0 removes the convenience yield and approaches the
% flat-demand boundary).
\newcommand{\MThrWt}{3.0}
\newcommand{\MThrOmT}{0.10}
\newcommand{\MThrW}{2.7}
\newcommand{\MThrLS}{-0.30}
\newcommand{\MThrLevy}{+0.03}
\newcommand{\MThrChiFall}{19}
\newcommand{\MThrOmA}{0.133}
\newcommand{\MThrOmB}{0.135}

% ---- Two-asset announcement transition (table: twoasset_transition) ----
% Frictionless two-asset economy, T=120, nx=220, ne=7. Sequence-space Newton
% with two clearing conditions per date; converged (3 Newton steps per
% continuation stage). The terminal prices reproduce the independently
% solved steady-state financing experiment of twoasset_step0 (dlnP +0.0085,
% q 2.3679) to four decimals.
\newcommand{\TwoTrT}{120}
\newcommand{\TwoTrResid}{6.6\times10^{-5}}
\newcommand{\TwoTrTermGap}{2.0\times10^{-8}}
\newcommand{\TwoTrIdent}{4\times10^{-14}}
\newcommand{\TwoTrImpP}{+0.75}
\newcommand{\TwoTrLongP}{+0.84}
\newcommand{\TwoTrShare}{89\%}
\newcommand{\TwoTrImpQ}{+1.24\%}
\newcommand{\TwoTrLongQ}{+1.10\%}
% One-asset front-loading share, for the comparison sentence.
\newcommand{\OneTrShare}{77\%}

% ---- Deficit-financed announcement path (table: transition_deficit) ----
% Indexed real program held identical to the balanced benchmark; only the tax
% path differs, tau_t = rbar*b_t + (1-rho_d^t)*g, and under this no-recovery
% rule the terminal nominal stock also ends kappa_inf higher -- a JOINT
% timing-and-ratchet experiment, not pure timing. Real debt is the residual;
% the terminal price floats at kappa_inf times the balanced terminal by
% nominal neutrality. na=500, T=80, 61 iterations; interior residual 4.3e-4,
% terminal (horizon) residual 1.1e-4; converged AND horizon-adequate.
\newcommand{\DefRho}{0.90}
\newcommand{\DefHalfLife}{6.6}
\newcommand{\DefKappa}{1.19}
\newcommand{\DefLnKappa}{+0.177}
\newcommand{\DefBal}{-0.040}
\newcommand{\DefImp}{+0.142}
\newcommand{\DefWedge}{+0.182}
\newcommand{\DefCap}{1.03}
\newcommand{\DefFrontBal}{0.77}
\newcommand{\DefFrontDef}{1.15}
\newcommand{\DefCumGap}{0.19}
\newcommand{\DefRevalBal}{-5.3}
\newcommand{\DefRevalDef}{+17.2}

% ---- Finite-horizon operativeness of the ordering (table:
% transition_ordering). Post-processing of the two converged, reportable
% paths (lump-sum vs levy-plus-rebate, nominal regime, na=500, T=80); no
% new solve. Delta_t = ln phat_rebate(t) - ln phat_lumpsum(t).
\newcommand{\OrdImp}{+0.161}
\newcommand{\OrdInf}{+0.151}
\newcommand{\OrdOne}{107}
\newcommand{\OrdTwo}{106}
\newcommand{\OrdFive}{103}
\newcommand{\OrdTen}{101}
\newcommand{\OrdTwenty}{100}
\newcommand{\OrdFlowImp}{+17.1}

% ---- Financing-timing ladder (table: transition_deficit_ladder) ----
% Same economy and program at every row; only the phase-in speed rho_d
% varies. The rho_d = 0 row is a regression test: phi == 1 collapses the
% deficit branch to the service rule analytically, and it reproduced the
% balanced benchmark impact response to five decimals (gap +0.00000).
\newcommand{\DefValidGap}{0.00000}
\newcommand{\DefRhoA}{0.50}\newcommand{\DefKapA}{1.019}
\newcommand{\DefPA}{-0.012}\newcommand{\DefWA}{+0.028}\newcommand{\DefCA}{1.52}
\newcommand{\DefRhoB}{0.70}\newcommand{\DefKapB}{1.044}
\newcommand{\DefPB}{+0.018}\newcommand{\DefWB}{+0.058}\newcommand{\DefCB}{1.33}
\newcommand{\DefRhoC}{0.80}\newcommand{\DefKapC}{1.078}
\newcommand{\DefPC}{+0.052}\newcommand{\DefWC}{+0.092}\newcommand{\DefCC}{1.22}
\newcommand{\DefRhoD}{0.90}\newcommand{\DefKapD}{1.193}
\newcommand{\DefPD}{+0.142}\newcommand{\DefWD}{+0.182}\newcommand{\DefCD}{1.03}
% Marginal pass-through of dilution into the impact price, first and last
% ladder interval: d(dlnP_1)/d(ln kappa_inf). Falls through one between
% rho_d = 0.8 and 0.9 -- the mapping is concave.
\newcommand{\DefSlopeFirst}{1.52}
\newcommand{\DefSlopeLast}{0.89}
% Solved threshold: bisection on the sign of the impact response.
% RENAMED round 10. These describe the critical speed of the JOINT
% timing-and-terminal-debt experiment, not a tax-timing threshold. The
% suffix exists so no call site can quote them as pure tax timing.
\newcommand{\DefRhoStarRatchetLo}{0.600}
\newcommand{\DefRhoStarRatchetHi}{0.613}
\newcommand{\DefRhoStarRatchet}{0.61}
\newcommand{\DefHalfStarRatchet}{1.4}
\newcommand{\DefCumStar}{1.5}
\newcommand{\DefKapStar}{3.2}

% ---- KV two-asset announcement path (table: twoasset_kv_transition) ----
% Ownership + illiquidity economy (lambda = 0.15, iota_H = 0.273), full grid
% n_b=60, n_k=34, n_e=8, T=60. Value-function backward induction (one Bellman
% application per date on the (b,k,e) grid) + per-date sparse distribution
% push; Anderson-accelerated fixed point on the stacked [log P; log q] paths.
% MEASURED SOLVER FLOOR, from three runs at different budgets and damping
% settings (140 it; 300 it; 300 it with adaptive damping). Best interior
% residual ranged 7.9e-4 to 9.5e-4 with the BOND block binding throughout,
% and the impact price ranged -0.0181 to -0.0172 -- a spread of 9e-4, the
% same order as the residual. So d ln P_1 is pinned to about +-5e-4 and the
% front-loading share to about +-0.02. QUOTE NOTHING FINER. The three runs
% gave front-loading 0.802, 0.758, 0.787; the paper reports 0.78 with the
% band, not a third digit. Reaching 1e-6 needs a sequence-space Newton with
% the two-market Jacobian; more Anderson iterations do not help, and
% over-damping (xi down to 0.016) produced a WORSE residual.
%
% SCOPE. Like the steady-state financing experiment it is anchored to, this
% path holds damages at D_0 and varies only the tax: it is the announcement
% of the FINANCING PROGRAM, isolating the portfolio/incidence channel that
% the restoration result is about. The one-asset tier-2 path, by contrast,
% carries the damage dividend as well.
% Gates (all PASS): Bellman step is a fixed point of V_term to 8.1e-07
% (tol 9.2e-06); forward push is a fixed point of the stationary
% distribution to 9.7e-12; kernel aggregates match the independent
% stationary construction to machine precision (|dSb| 1.1e-16, |dSk| 0);
% end-to-end no-program constant-price recursion reproduces the baseline
% (bond 4.2e-07; the tree entry 1.2e-03 is inherited from the BASELINE
% equilibrium's own q-bisection tolerance, not from the path kernels).
\newcommand{\KVTrT}{60}
\newcommand{\KVTrLam}{0.15}
\newcommand{\KVTrIota}{0.27}
\newcommand{\KVTrResid}{9.5\times10^{-4}}
\newcommand{\KVTrTerm}{3.3\times10^{-4}}
\newcommand{\KVTrMargin}{2.1}
\newcommand{\KVTrGateB}{8.1\times10^{-7}}
\newcommand{\KVTrGateK}{9.7\times10^{-12}}
\newcommand{\KVTrImpP}{-1.8}
\newcommand{\KVTrLongP}{-2.26}
\newcommand{\KVTrShare}{0.78}
% tree: impact +0.0067 with a solve-implied bound |dln q| < 2.7e-3, so the
% impact/long-run share (52%) carries about +-21pp. Direction only.
\newcommand{\KVTrQBound}{4.0\times10^{-3}}
\newcommand{\KVTrElas}{-0.17}
\newcommand{\KVTrImpBand}{5\times10^{-4}}
\newcommand{\KVTrShareBand}{0.02}

% ---- KV transition-inclusive incidence (MC3 deliverable) ----
% Consumption-equivalent transfer, baseline-wealth groups, exact
% mass-splitting quantile bands (masses hit 0.2000/0.5000/0.1000/0.0100
% exactly). CE defined on 1.0000 of mass in both columns.
% NOTE ON LEVELS: damages are held at D_0, so the program delivers NO
% damage dividend here and every group loses; only the cross-group gradient
% and the transition-vs-steady-state contrast are comparable to the
% one-asset numbers.
\newcommand{\KVWelBot}{-4.38}\newcommand{\KVWelBotSS}{-4.40}
\newcommand{\KVWelHalf}{-3.73}\newcommand{\KVWelHalfSS}{-3.77}
\newcommand{\KVWelMid}{-2.48}\newcommand{\KVWelMidSS}{-2.57}
\newcommand{\KVWelTop}{-1.25}\newcommand{\KVWelTopSS}{-1.35}
\newcommand{\KVWelTopOne}{-0.03}\newcommand{\KVWelTopOneSS}{-0.12}
\newcommand{\KVWelCon}{-5.15}\newcommand{\KVWelConSS}{-5.17}
\newcommand{\KVWelWHtM}{-3.94}
\newcommand{\KVWelAll}{-2.98}\newcommand{\KVWelAllSS}{-3.05}
\newcommand{\KVWelMaxGap}{0.10}
\newcommand{\KVWelOneAssetGap}{0.8}

% ---- Wealth-mobility diagnostics (table: validation_mobility) ----
% Stationary transition operator (Young lottery x income mixing) on the
% calibrated benchmark, na=500, ne=7, beta=0.9267. All four
% definition-robust stylized facts satisfied (low short-run mobility,
% top most persistent, persistence declining in the horizon, U-shaped
% profile). Quantitative comparison to published matrices NOT performed:
% it requires reconciling the model's single liquid asset with survey net
% worth, and the data slots in the driver are deliberately left empty.
\newcommand{\MobQoneTwo}{0.81}
\newcommand{\MobQfiveTwo}{0.91}
\newcommand{\MobQoneFive}{0.63}
\newcommand{\MobQfiveFive}{0.78}
\newcommand{\MobShTwo}{0.30}
\newcommand{\MobShFive}{0.58}
\newcommand{\MobConMass}{20\%}
\newcommand{\MobConStay}{81\%}

% ---- Matched-parameter ladder (table: twoasset_ownership_kv_ladder) ----
% Coarse grid. beta = 0.874 and chi_b = 0.00223 held at the benchmark values
% in every row; grids identical; one ingredient switched off per row.
\newcommand{\LadBenchLS}{-0.011}
\newcommand{\LadBenchLevy}{+0.012}
\newcommand{\LadBenchTopOne}{33}
\newcommand{\LadNoWedgeLS}{+0.003}
\newcommand{\LadNoWedgeLevy}{+0.008}
\newcommand{\LadNoWedgeTopOne}{34}
\newcommand{\LadNoSupLS}{+0.034}
\newcommand{\LadNoSupLevy}{+0.061}
\newcommand{\LadNoSupTopOne}{6}
\newcommand{\LadNoFricLS}{-0.061}
\newcommand{\LadNoFricLevy}{-0.033}
\newcommand{\LadNoFricTopOne}{71}
