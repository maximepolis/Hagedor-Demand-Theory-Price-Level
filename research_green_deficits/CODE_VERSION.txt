R11.45
# One line, above, is the pipeline revision. Everything below is a comment.
#
# Bump this whenever a driver's INTERFACE or required inputs change, so that a
# pasted console log identifies which revision produced it. This exists because
# a stale main_baseline_capture.m -- one that still required a git worktree and
# a BASELINE_OUT variable -- was run against a set of instructions written for
# the rewritten one, and the only symptom was an assertion about a variable
# that the current file does not mention. The revision is now printed by every
# driver in the parity sequence, next to that file's own modification date.
#
# R11.45 the D10 parity refused to run for want of output/baseline, which is
#        correct behaviour and which I had set the operator up to hit: the
#        R11.44 run list named the parity drivers without their prerequisite.
#        No code was wrong. What was missing was that output/baseline is a
#        LOCAL artifact -- untracked, and by EXTRACT_RULE.md's own principle it
#        must stay that way, since results flow machine -> repo and never back
#        -- so a fresh extract never has it and the refusal recurs every time.
#        Both parity drivers now say that in the refusal itself: expected after
#        a download rather than a defect, one main_baseline_capture writes BOTH
#        legs, and add FAST to match. EXTRACT_RULE.md gains the section.
#        Verified while diagnosing, because the answer decides whether the
#        R11.44 VFI hoist is testable at all: baseline_bf0a4e8/src/
#        solve_household_vfi.m carries NO precompute, and main_baseline_capture
#        puts that frozen tree first on the path with addpath(genpath(BSRC),
#        '-begin'). So the baseline is produced by the PRE-hoist solver and the
#        comparison by the current one, and building the baseline fresh today
#        cannot contaminate the test. Also checked the failure mode that would
#        have wasted the run: the baseline filename is FAST-tagged and the
#        driver asserts BASE.env.fast == FAST, so a settings mismatch reports
#        as "not found" or as an explicit assert, never as a spurious field
#        difference that would read as a refactor failure.
# R11.44 OPTION A adopted. The two-asset specifications now report the
#        financing ORDERING and explicitly decline the level, in the abstract,
#        the section that used to be titled "Ownership plus illiquidity
#        restores the one-asset sign" (now "preserves the financing
#        ordering"), the two-by-two ladder reading, the Schur status line, the
#        transition scope paragraph, the wealth-matched robustness note and
#        the zeta appendix title. The scope restriction is stated once, up
#        front, in the section's own opening rather than as a footnote to each
#        number: on the discretizations we can solve, the two-asset level
#        response is NOT RESOLVED and a reader should not weigh it against the
#        one-asset magnitudes at all.
#        Two efficiency findings, one of which was also blocking the figures.
#        (a) solve_regime_equilibrium discarded the household block on every
#        one of the ~19 solves in its bisection, keeping two scalars. S_green
#        already returns the stationary distribution, both policies and the
#        value function on every call. The closure now caches the last
#        feasible one and the root exposes it as eq.hh -- no extra solve, one
#        struct assignment -- so the distribution the paper is about survives
#        the aggregation. main_project_regimes stores it per regime.
#        (b) solve_household_vfi rebuilt the na x na consumption matrix and
#        called utility() on it, once per income state, on EVERY sweep. None
#        of r, tau, the grids or sigma changes during a solve, so those were
#        two million power evaluations per state per iteration producing the
#        same numbers each time. Hoisted to a precompute: ~16 MB at na = 500,
#        ne = 8, and the dominant per-iteration cost removed from both the
#        Bellman step and the Howard sweeps. Bit-identical by construction --
#        same expression, same inputs, evaluated once -- and D10/D11 parity is
#        the check that this holds in fact and not only in argument.
#        New main_hank_heterogeneity + PFig23 answers one question: when the
#        same real program is financed lump-sum rather than by a levy, WHICH
#        households change their demand for nominal debt. Four panels: the
#        ergodic wealth distribution by regime; the cumulative decomposition
#        of dS across the wealth distribution, whose endpoint is the aggregate
#        change and whose shape says who supplies it; the MPC by wealth and
#        income state, with the raw range reported so a clipped axis cannot
#        hide a bad policy; and the Lorenz curve of bond holdings, which in a
#        one-asset economy IS the ownership distribution the incidence claim
#        is about. It solves nothing -- it reads what the regime run now keeps.
# R11.43 the widened Track A ran and its FIRST cell settled the question the
#        whole matrix was for. Widening fixed the ILLIQUID truncation (gates
#        8 and 9.1 now pass, having failed on the narrow family) and did NOT
#        fix the LIQUID one: gate 7 improved 11x, from 0.00275 to 0.000246,
#        and still fails 1e-4, while gate 9 -- the highest OCCUPIED node as a
#        fraction of the ceiling -- is unchanged at exactly 1.0000. There is
#        still mass above 1e-8 at b = 96, eight times the original ceiling.
#        That contrast is precisely what the gate pair was built to detect:
#        kv_boundary_mass's header says a small top-two-node mass "on its own
#        proves nothing" because widening shrinks it mechanically, and that
#        ks small with ko ~ 1 is "a distribution still leaning on the wall
#        through a thinner slice of it". With the superstar state the liquid
#        tail is genuinely long, not clipped -- not a ceiling one can afford
#        to raise.
#        Two independent disqualifications in the same cell: target drift
#        +0.1537 on a 0.30 target (S_b = 0.4537, against at most 4.6% drift
#        on the narrow family), so the widened economy is not the calibrated
#        one and its dP spread would not be a discretization measurement; and
#        the residual gates fail by one to two orders of magnitude.
#        Recorded in TWO_ASSET_FAILURE_FALLBACK_MEMO.md section 1.3. This
#        closes the question section 6 said nothing should be chosen before.
#        Option A now rests on two independent measurements. It does NOT
#        reject Option B in general -- only its cheapest form, buying
#        certification with a bigger grid.
#        The memo also refuses, explicitly and in writing, the shortcut the
#        pre-flight offers: scoring gates 7-9 once per family rather than per
#        cell. Defensible as bookkeeping, inadmissible as a route to
#        certification -- the mass is really at the wall, and scoring the
#        gate once does not move it. No threshold or scoring rule changes on
#        the strength of a run that failed.
# R11.42 the widened Track A run printed a warning saying it could not
#        certify anything -- while running the experiment that decides
#        whether it can. The pre-flight famfail block was written before
#        WIDEN existed and asserts unconditionally that "Gates 7-9 are
#        properties of the grid EXTENT, which Track A holds FIXED. Every
#        cell below will fail them with the same values, and no cell can be
#        certified." Under WIDEN that is false in every clause: the extent
#        was just multiplied by 8 in b and 6 in k, and kv_gate_report
#        re-measures gates 7-9 per cell on the CELL's own grid
#        (CTX.p.bGrid / CTX.p.kGrid), not on the frozen benchmark's. The
#        message now branches: under WIDEN it says these are the narrow-family
#        values and the REASON for widening, and points the reader at the
#        per-cell gate blocks; the old text is kept verbatim for WIDEN =
#        false, where it is exactly right. Telling an operator four hours in
#        that the job is futile, when it is not, is the one kind of wrong
#        that destroys the result rather than merely misreporting it.
# R11.41 the clean ledger exposed a claim the manuscript could not support.
#        Five sites said the financing ordering holds "at BOTH ENDS of the
#        disciplined convenience-yield curvature". The disciplined range is
#        an elasticity range [-2.05, -0.55]; the two curvatures actually
#        solved are the benchmark zeta = 2 (implied elasticity -1.82, 16% of
#        the range width from the ELASTIC end) and zeta = 1, the KVJ POINT
#        ESTIMATE, which is interior. The flat end near zeta = 0.55 was never
#        solved, so "both ends" claimed coverage of an endpoint that does not
#        exist in the computed set -- and it did so inside the ROBUST-claim
#        display quote, its verbatim restatement in the conclusion, and the
#        paragraph whose whole function is to bound what the section claims.
#        Corrected at all five sites in one pass: a single-site rewrite would
#        have desynchronised the display quote from its own conclusion.
#        Line 2678 was wrong twice -- it also pointed at Appendix subsec:zten
#        for the Stone-Geary and grid checks, which that appendix does not
#        contain; they are reported in the body beside the restoration result.
#        The manuscript's primary mapping statement hard-typed -0.75, 0.73 and
#        [-2.05, -0.55] and hedged the benchmark as "at its steep edge". Those
#        are all machine-derivable, so export_paper_numbers now emits
#        \KvjElModel, \KvjElPoint, \KvjElLo, \KvjElHi and \KvjElPct from
#        convenience_kvj.mat and twoasset_ownership_kv.mat, guarded on both
#        files and -- for the position -- on the model value actually lying
#        inside the range, since "percent of the way through" is nonsense
#        outside it. The prose now states the measured position and says why
#        position beats distance: the range is asymmetric about its point
#        estimate, which is exactly the approximation that manufactured the
#        FLAG fixed in R11.40.
#        An adversarial pass rejected the first draft of five of these edits
#        for spending macros that did not exist yet -- LaTeX that would not
#        have compiled. The macros were built first.
#        Recorded in the register: new rows 13a-13c for Major 2 items 1-3
#        (item 1 DISCHARGED, item 2 DISCHARGED and answered past the ask,
#        item 3 SET UP but not performed while 12 of 13 data slots are
#        empty). Item 8's two-asset leg is deliberately HELD at WP rather
#        than moved to RO: Track A's ordering certification held the grid
#        BOUNDS fixed and varied only node counts, so it cannot separate a
#        density problem from a boundary problem, and the widened family
#        authorised on 2026-08-04 has never produced a Gate 11 verdict --
#        the completed matrix ran with WIDEN = false.
# R11.40 the ledger's one FLAG was manufactured by a field name I got wrong.
#        Section C graded the demand-curve log-elasticity against a symmetric
#        half-width and reported FLAG at -1.8174 vs -1.0274 -- while
#        calibrate_convenience_kvj's own console prints "the benchmark
#        zeta = 2 lies INSIDE the KVJ range (log-elasticity ~ -1.8 vs bound
#        -2.05)". Both cannot be right. The driver COMPUTES kvj_log_lo and
#        kvj_log_hi at line 134 and then omits them from its save list, which
#        carries kvj_lo/kvj_hi -- the raw pp SEMI-elasticity bounds, which the
#        driver's own comment says are not comparable to a model whose spread
#        sits at equity scale. My ledger read the log field names; the writer
#        never saved them; the interval branch never fired. The save list now
#        persists what the console claims.
#        The deeper defect was the SILENCE. Falling back from a published
#        range to a symmetric band is a reasonable degradation, but here the
#        two rules give OPPOSITE verdicts, and the ledger took the fallback
#        without a word -- printing a rejection that contradicted the source
#        driver. Both section C and V3 now announce the demotion and say the
#        FLAG must not be read as a rejection until the range is restored.
#        Confirmed in the same run: all four sources load, V1 is measured
#        (9 configs, 3 ties, top-10 spread 0.0137), and the banner is quiet
#        now that absent is distinguished from disagreeing.
# R11.39 the ledger reported V1 and V3 STALE again, for the THIRD time, and
#        the cause was never the ledger. output/ is tracked -- 205 files,
#        every .mat among them -- and the committed copies are old:
#        wealth_fit_results.mat and convenience_kvj.mat date from PR #155 and
#        predate IDENT and kvj_logel. So unpacking the archive over the
#        working folder OVERWRITES freshly computed results with stale
#        committed ones. Run the fit, get IDENT, re-extract to pick up new
#        drivers, lose IDENT, and the ledger correctly reports the file is
#        short -- while the run that filled it happened yesterday. From
#        inside MATLAB a clobbered file and a never-generated one are
#        identical, which is why the detector kept being right about the
#        state and silent about the cause. New EXTRACT_RULE.md states the
#        one rule that prevents it: extract the CODE, never output/ or
#        paper/numbers_auto.tex. Those flow the other way.
#        Also fixed the false alarm that ran on every banner for two days:
#        kv_code_version compared its not-found PLACEHOLDER against EXPECTED,
#        declared MIXED EXTRACT, and advised deleting a file that was not
#        there. Absent and disagreeing are different conditions -- absent
#        means the .m constant is the only source, which is a complete
#        answer and gets one quiet note; present-but-different is a real
#        mixed extract and still shouts.
#        Confirmed working in the same run: section C is 13 rows, the
#        duplicated convenience-yield/equity-premium row is gone, and V7
#        prints both algebraic forms agreeing at 5.9472 pp.
# R11.38 check_output_staleness's extract guard was wrong, and its own first
#        run disproved it. All fourteen flagged writers carried the identical
#        stamp 2026-08-07 12:04 -- one unpack event, not fourteen edits --
#        yet the guard announced "the .m stamps span 116.1 hours, so the
#        comparison above is meaningful". It had compared the OLDEST and
#        NEWEST source in the tree, a span kept wide by files left from
#        earlier extracts, when the question is whether THIS writer's stamp
#        dates an edit. False reassurance is worse than no check: the reader
#        stops looking.
#        The guard is now per-row and measures CLUSTERING -- how many other
#        sources share this writer's timestamp to the minute. Files are edited
#        singly and unpacked in batches, so a shared stamp dates an unpack.
#        Those rows report cannot-tell, never STALE, and the summary says
#        plainly that a tree assembled entirely by extraction yields no stale
#        verdicts as a LIMIT OF THE EVIDENCE rather than a clean bill of
#        health.
#        What the audit cannot see, and is true: identification_ledger.mat is
#        stamped 2026-08-06 22:25, before R11.30 collapsed the duplicated
#        convenience-yield row, added verdict V7 and switched the
#        log-elasticity to interval grading. That artifact is genuinely out of
#        date with its driver, and only the run log knows it.
# R11.37 the regenerated PFig18 exposed two defects, one of them mine.
#        (a) I wired its third path to regime_style('reversal') on the
#        assumption that it was a policy-reversal path. The argument doc one
#        screen above says TRr is the LEVY-PLUS-REBATE design. So the alias
#        drew the rebate regime amber here and purple in every financing
#        figure -- precisely the drift the registry exists to prevent,
#        introduced by guessing a series' meaning instead of reading it. The
#        'reversal' alias is deleted, not repointed: an alias invented to fit
#        a call site rather than to name a meaning defeats the registry.
#        (b) The nominal and indexed price paths very nearly COINCIDE in
#        panels (a) and (b) -- that near coincidence is a result -- so with
#        both drawn solid the second painted over the first and the figure
#        appeared to show two series while plotting three. regime_style has
#        always returned a line style for exactly this; the plot calls were
#        taking only the colour. They now take both, which also makes the
#        overlap survive black-and-white printing. Panel (d) plots damages,
#        not a financing regime, and now uses the damage colour so it cannot
#        be misread as a fourth policy path.
#        Separately, main_deficit_estimands closed by telling the reader that
#        re-locating the critical speed "requires sweeping rho under the C2
#        rule" -- work that had already run and was sitting in
#        timing_frontier.mat beside it. It now READS that file and reports
#        both frontiers, falling back to the request only when the sweep is
#        genuinely absent. A driver demanding a run that has finished is the
#        stale-verdict failure of R11.35 one file over.
# R11.36 the collaborator's machine has MATLAB and NO Python, restated. Two
#        places violated that: replot_stale_figures ENDED by printing
#        "Then: python3 paper/check_output_staleness.py" as the next step --
#        a driver instructing a MATLAB-only reader to run Python -- and
#        RUN_THEN_RESPOND.md offered check_manuscript_numbers.py under the
#        heading "run locally anytime; no MATLAB needed", which is precisely
#        backwards for this machine. Both now say MAINTAINER SIDE, and the
#        run doc states the standing rule: nothing in the run list requires
#        Python, and a driver that prints a python3 next step is a bug.
#        New check_output_staleness.m ports the audit so the one check that
#        belongs in the RUN loop can actually be run there. It answers the
#        same question by DIFFERENT evidence, which is stated in the file
#        rather than glossed: the Python version dates files by last git
#        commit, unavailable here because the project arrives as an extracted
#        archive with no repository, so this one compares modification times.
#        That carries a failure mode the file names and handles instead of
#        hiding -- a fresh extract stamps every .m at once, so every earlier
#        artifact would look stale. When the .m stamps are bunched within an
#        hour the function DEMOTES its own stale verdict to "cannot tell"
#        and says why. It also reports the timestamp-free half first: which
#        required artifacts do not exist, with the driver that writes each.
# R11.35 the re-run closed the timing bracket: rho* = 0.7977, a 3.07-year
#        tax half-life, bracketed on [0.70, 0.80] with no gap -- 2.2x the
#        joint frontier's 1.41 years. The repaired rho = 0.80 speed solved
#        in 9 transitions off the warm start (C2 = +0.000260); 0.85 took 12.
#        The provisional 2.62 y from the gapped bracket was biased downward
#        exactly as the convexity argument predicted. Both frontier
#        locations are now QV in the claim register.
#        One export defect found by reading the emitted numbers_auto.tex:
#        the frontier macros appeared but the \DefDec* decomposition macros
#        did not. Cause: the exporter trusted EST.reportable inside
#        deficit_decomposition.mat, which was frozen at FALSE by the
#        pre-R11.26 gate -- the unattainable absolute eps_kappa -- while the
#        run it stores passes the amended ratio gate. main_deficit_estimands
#        is the driver that re-applies the amended gates to the saved run,
#        and it writes its re-graded verdict to deficit_estimands.mat. The
#        exporter now reads THAT file first and falls back to the
#        decomposition's own EST only when the estimands file is absent.
#        A verdict frozen at an old gate is the .mat-staleness lesson of
#        R11.29 in a new place: the flag loaded cleanly and was simply
#        wrong about the current acceptance rule.
# R11.34 the R14 co-editor brief, triaged rather than re-run. Most of its
#        sections are already standing practice (the identification ledger IS
#        its section-N table; the C1/C2/C4 block and fixed-real-program run
#        ARE its section-O experiments 3-5; the demotion map IS its
#        extension rule), and REFEREE_R14_COEDITOR_TRIAGE.md records the
#        mapping, the author decisions it drafts but does not take (ten
#        titles, three abstracts, an intro outline), and the two things it
#        rejects with reasons (a one-asset resolution ladder that would
#        reassure where the problem is not; a merged mega-figure that would
#        blur what each panel holds fixed).
#        Implemented now: the brief's one genuinely new mechanical
#        requirement, colour-by-MEANING figures. style_figure.m standardizes
#        typography but assigns palette by series position; six plot files
#        declared private RGB constants, so a colour could change meaning
#        between exhibits. New src/regime_style.m is the paper-wide semantic
#        registry (benchmark grey, program green, lump-sum blue, levy
#        vermillion, rebate purple, deficit amber, timing sky; supply blue /
#        demand vermillion in the market diagrams), each meaning with a B/W
#        line style, erroring on unknown names so no figure can invent an
#        encoding. All six plot files now source their constants from it;
#        plot calls untouched, so restyling cannot change what any figure
#        reports. Main-text exhibit count verified at 5 figures + 5 tables,
#        already inside the brief's budget.
# R11.33 the full-grid frontier run came back: JOINT frontier rho* = 0.6121,
#        a 1.41-year tax half-life, tightly bracketed on [0.60, 0.65] -- the
#        manuscript's quoted 1.4 years is thereby VALIDATED, as the joint
#        frontier. The TIMING frontier is provisional: the rho = 0.80 speed
#        failed and the printed 2.62 y interpolates a convex curve across
#        [0.70, 0.90], biased downward; the true crossing sits above 0.80.
#        Diagnosis of the failure: the consolidation expansion's first probe
#        a_xi = 0.05 is 4x-28x above every measured root; the transition at
#        that oversized surcharge did not converge, and the expansion read
#        "this amplitude failed" as "no bracket exists". Feasibility is not
#        monotone in a (0.05 solves at rho = 0.90), so a failed probe says
#        nothing about amplitudes below it. kv_solve_consolidation now
#        SHRINKS toward the feasible side on an infeasible probe, tracks the
#        smallest known-bad amplitude and never expands past it, and accepts
#        an opt-in consolidation_a0; the sweep warm-starts each speed from
#        the nearest solved neighbour's a_xi. Default probe unchanged, so a
#        run that never hits an infeasible point replays identically.
#        An adversarial review of these fixes found four defects before the
#        re-run could pay for them: (1) the joint frontier interpolated
#        through dP_C4 values from rows whose C4 never converged -- finite
#        numbers off unsolved paths; C4 now filters on conv_C4 exactly as C2
#        filters on status; (2) the bisection could return an a_xi that is
#        known-infeasible while kappa_inf came from a different amplitude --
#        the reported triple is now kept consistent; (3) the PROVISIONAL
#        rule compared bracket width to the tightest grid gap, so the
#        deliberately non-uniform default grid would have false-alarmed --
#        the rule is now the property it meant to detect, a REQUESTED speed
#        inside the bracket with no usable row; (4) an exact zero at a grid
#        point read as "no sign change". Cached FAILED rows are retried, not
#        replayed: a checkpoint must never checkpoint a refusal, and a
#        failed row's converged C4 half is reused so only the consolidation
#        is repaid. Default grid now carries 0.80 AND 0.85.
#        Integration: main_deficit_decomposition's estimands and the
#        frontier flow into the paper through export_paper_numbers as
#        \DefDec* / \DefRhoStarTiming / \DefHalfStarTiming /
#        \DefTimingJointRatio / \DefHalfStarJointSweep, guarded on
#        EST.reportable and on the sweep's own gap-free-bracket flags, so a
#        provisional crossing renders \pendingnum and cannot reach the text
#        as a number. The manuscript's "which we do not compute here" caveat
#        now reports the computed decomposition; the intro's "defer" clause,
#        the conclusion's "is not computed", the \emph{only timing} setup
#        sentence, the ladder caption, and the numbers_manual comment are
#        all reworded to the joint label. CLAIM_STATUS_R13 item 18, the
#        response plan's Major 9 rows and section 7, and the architecture
#        plan's intro/summary specs record the run and the licensed line.
# R11.32 the pilot run located the TIMING frontier and, just as usefully,
#        failed to bracket the JOINT one. C2 crosses zero at rho* = 0.7476,
#        a 2.38-year tax half-life; C4 is still POSITIVE at rho = 0.65
#        (+0.0066), so its crossing lies below the grid -- consistent with the
#        manuscript's 1.4-year figure, rho = 0.61. The gap is the finding the
#        decomposition could not produce: the manuscript quotes the JOINT
#        frontier, and flipping the sign on TIMING ALONE takes a delay about
#        1.7x longer.
#        kv_zero_cross now returns WHY, and specifically which side the
#        crossing must lie on -- "every value is positive, extend downward" is
#        what the next run needs; "not bracketed" leaves the reader to derive
#        it. The default grid is rebuilt from the pilot: dense on [0.55, 0.80]
#        where both crossings are, stopping at 0.90 rather than spending a
#        speed at 0.95 where neither frontier can be.
#        New cross-check against main_deficit_decomposition at rho = 0.90.
#        The two runs solve the same three experiments by different routes --
#        the decomposition's bisection stopped on its iteration cap chasing
#        kappa_tol = 1e-8, this one stops on eta against the ratchet -- and
#        the pilot already agrees to 3.5e-05 on C2 and exactly on C1 and C4.
#        That validates the cheaper stopping rule on a case that was already
#        certified, and it costs nothing: the answer is on disk.
# R11.31 the FAST frontier run died on `assert(TR1.converged)`, which reports
#        a solver outcome as though it were a driver bug. It is neither:
#        main_deficit_decomposition already established that these transition
#        paths do not converge at the shakeout horizon, and stops there with
#        that named as the expected cause. main_timing_frontier now does the
#        same -- prints the residuals, names FAST, writes the file, returns.
#        Three further fixes the crash exposed, all of which would have cost
#        real time on the overnight run:
#        (a) the checkpoint signature included RHOGRID, so a two-point pilot
#            and the six-point production run had different signatures and the
#            pilot's speeds were discarded. Speeds are independent solves;
#            the signature now covers the ECONOMY and solved speeds are stored
#            and matched one at a time, in a CELL so that adding a diagnostic
#            field cannot make an old checkpoint throw on append. A pilot now
#            seeds the full run. C1 is cached too, and checkpointed before the
#            first speed rather than after it.
#        (b) C1's rho_bar was read from RHOGRID(1). C1 does not depend on rho
#            -- phi_t = 1 at every date -- but taking it from the grid made
#            the cached C1 depend on the grid it was solved under. Fixed at
#            0.90.
#        (c) a non-converged C4 no longer starts a consolidation bisection.
#            a_xi = 0 IS C4, so the first evaluation is the path that just
#            failed and the solver refuses to bracket through it -- after
#            paying for the solve.
#        Finally, the extract problem is now provable rather than suspected.
#        Three extractions delivered R11.26, R11.28 and R11.30 drivers while
#        every banner read "pipeline R11.19": the unpacker refreshes the .m
#        files and leaves the text file alone. kv_code_version now carries the
#        revision as a constant and reports a MIXED EXTRACT when it disagrees
#        with CODE_VERSION.txt, treating the .m files as authoritative. New
#        paper/check_code_version.py fails if a bump lands in only one place.
# R11.30 the ledger printed one model number twice under two names. Section C
#        carried a "convenience yield (pp)" row, 100*[(q+d)/q - (1+r_b)], and a
#        "tree yield d/q minus r_b" row keyed as the equity premium. Those are
#        the same quantity identically, and the duplication is not a
#        bookkeeping slip: the liquid FOC chi_b v'(b') = u'(c)[1-(1+r_b)q/(q+d)]
#        makes the convenience yield and the tree's excess return the SAME
#        object, because the tree is the only alternative asset. So one number
#        must answer to two literatures that differ by an order of magnitude.
#        The rows are collapsed to one and the tension is promoted to verdict
#        V7, which states the identity, prints both algebraic forms so they can
#        be seen to agree, and grades the single number against each data slot
#        in turn. Like V2 it binds without an external number; transcription
#        sizes it and cannot rescue it.
#        Second: the demand-curve log-elasticity was graded against HALF THE
#        WIDTH of the KVJ range centred on the point estimate, and reported
#        FLAG at -1.8174 against -1.0274. The KVJ range is asymmetric, roughly
#        [-2.05, -0.55], and the model value sits inside it -- 16% up from the
#        elastic end. The symmetric approximation manufactured the rejection.
#        Section C now grades against a published RANGE when the source .mat
#        carries one, and V3 reports where in the range the model sits. The
#        recommendation is unchanged and is now unconditional: report the
#        zeta = 1.0 rerun alongside the benchmark.
# R11.29 a stored .mat that LOADS is not the same as one that is CURRENT. The
#        ledger run of 2026-08-06 reported V1 as "NOT YET MEASURED" and the
#        KVJ counterpart in V3 as absent, and both readings were correct about
#        the file and wrong about the economy: wealth_fit_results.mat and
#        convenience_kvj.mat both predate the variables those verdicts read,
#        so they loaded cleanly and were simply short. "NOT YET MEASURED"
#        reads as a finding; it was a property of the extract. The ledger's
#        source table now carries the list of variables each downstream
#        verdict dereferences and reports a loaded-but-short file as STALE,
#        names the missing variables, and prints the re-run line, in the
#        header -- not four hundred lines down in prose.
#        The same run showed the second half of the failure: the banner read
#        "pipeline R11.19" while the driver itself was stamped that afternoon,
#        i.e. an extract that refreshed the sources and left CODE_VERSION.txt
#        behind. kv_code_version now compares the running driver's date with
#        the version file's and flags a gap of more than a day, so the banner
#        that exists to catch version mismatch can catch its own.
# R11.28 new main_timing_frontier: the decomposition licensed a tax-timing
#        claim in KIND (C1 negative, C2 positive at one speed) but the
#        manuscript reports a critical SPEED, and that frontier was located
#        on C4, which carries the ratchet as well as the delay. This sweeps
#        rho and locates both crossings: rho*_timing on C2 and rho*_joint on
#        C4. C1 is solved ONCE -- under the contemporaneous rule phi_t = 1 at
#        every date, so it does not depend on rho. Checkpoints per speed on a
#        signature, since each speed costs a full consolidation bisection.
#        The default grid spans 0.55 to 0.95 because the manuscript's 1.4-year
#        frontier is rho = 0.61 while the decomposition ran at 0.90, and the
#        grid must bracket both. New kv_zero_cross refuses to report a
#        frontier the data do not bracket.
# R11.27 the estimands driver reported the magnitude shares and MISSED the
#        decision rule. The referee's test for Major 6 is whether the SIGN
#        reverses between C1 and C2, not how large C2-C1 is relative to
#        C4-C1: a timing channel that is only a third of the magnitude can
#        still carry the whole sign reversal, and that is a licensed
#        tax-timing claim. On the real run C1 = -0.0396 and C2 = +0.0221 --
#        opposite sides of zero -- and the driver said only "both channels
#        are material". The sign test is now explicit, with the
#        one-phase-in-speed caveat: the manuscript reports a critical SPEED,
#        which is still a joint frontier until rho is swept under C2.
#        Also fixed a doubled prefix: kv_code_version already returns a
#        string beginning "pipeline", so two drivers printed "pipeline
#        pipeline R11.xx".
# R11.26 the benchmark C1/C2/C4 run PASSED every substantive gate -- both
#        paths converged, C4 reproduced kappa_legacy to 2.5e-05, and the
#        budget identities came in at 4.2e-14 (period) and 2.0e-15 (PV)
#        against thresholds of 1e-9 and 1e-7 -- and then withheld the
#        estimands on ONE criterion: the spec's absolute terminal-debt
#        tolerance eps_kappa = 1e-8. The bisection drove the miss to 2.2e-05
#        in 38 iterations and stopped improving, which is a floor set by the
#        transition's own noise (tol = 2e-3), not a bracket needing more
#        halvings. 1e-8 demands more precision in an OUTCOME than the solve
#        producing it has -- the same defect as gate 4's original 1e-8.
#        AMENDED: the gate is now the miss NORMALIZED BY THE RATCHET BEING
#        DECOMPOSED, |kappa^C2-kappa^C1|/|kappa^C4-kappa^C1| < 1e-3, the same
#        construction Gate 11 uses. Observed: 2.2e-05/0.1932 = 1.2e-04. The
#        absolute miss is still printed.
#        New main_deficit_estimands reads the SAVED .mat and reports the three
#        estimands without re-solving: the run takes 83 minutes and every
#        number needed was already on disk.
# R11.25 the first C1/C2/C4 run produced three misleading diagnostics from
#        ONE cause: neither C1 nor C4 converged at FAST, and the block carried
#        on regardless. The consolidation then failed at its first evaluation
#        (a_xi = 0 IS C4, and kv_solve_consolidation rightly refuses to
#        bracket through a non-converged point); the parity check compared a
#        non-converged kappa against the legacy reference and called 1.4e-3 a
#        failure; and the budget checker crashed dot-indexing the empty TR
#        that INFEASIBLE returns. Convergence is now a PRECONDITION with an
#        early stop that names FAST as the expected cause.
#        My kv_budget_identities was also simply wrong, which the 2.1
#        residuals were reporting honestly. It assumed b_{t+1} = (b_t + pg_t)/
#        (1+rbar). The solver carries the lagged stock at the REALIZED path
#        rate r_t, not rbar, and starts from b_0 = B0/P_0, the PRE-
#        ANNOUNCEMENT stock -- b_path(1) is already post-revaluation, so using
#        it as the initial condition drops the announcement-date revaluation
#        from the identity, i.e. exactly the object this paper is about.
#        Rewritten against the solver's loop rather than from memory, with
#        both the per-date and the accumulated form carrying r_t.
# R11.24 I set regime='nominal' in the wired C1/C2/C4 block. Supplying
#        opts.fiscal makes the solver treat the run as deficit financing, and
#        it then asserts regime='indexed'. The assertion is economics: the
#        experiment holds the REAL program fixed so ONLY tax timing differs
#        across cases, and under a nominal appropriation the terminal dilution
#        would shrink the real program, so C4's higher terminal debt would buy
#        a SMALLER programme than C1's and C4-C1 would mix timing, the ratchet
#        and a change in the size of the thing being financed. Set to
#        'indexed', with the reason recorded at the line so it is not tidied
#        back. The assert caught it before a single transition was solved.
# R11.23 main_deficit_decomposition was a SCAFFOLD: it built and froze the
#        C1/C2/C4 specifications, verified C3 is refused by the builder, and
#        then stopped with "Solving is not yet authorized: main_parity_d11
#        _deficit must run against the bf0a4e8 baseline and pass". That gate
#        HAS now passed (48 fields exact, all three legs), so steps 1-6 from
#        its own header are wired: solve C1 and C4 with kappa free; solve C2's
#        surcharge amplitude by kv_solve_consolidation against C1's realized
#        terminal debt; PARITY-check C4 against kappa_legacy; budget
#        identities V4 (period, normalized by program expenditure) and V5
#        (present value, normalized by initial debt) via the new
#        kv_budget_identities; then the three estimands, WITHHELD unless every
#        gate passes. It uses kv_legacy_transition_setup rather than
#        setup_params_green's default beta -- the defect that has broken two
#        drivers here. No interaction term is reported: with three feasible
#        paths and two margins it is not identified, and C3 (the cell that
#        would identify it) is infeasible under the contemporaneous rule.
# R11.22 Track B was billed as "recalibrated to the same targets" but passed
#        W_targ = [], which sends kv_calibrate_on_grid down the ONE-instrument
#        path: beta is re-fitted per grid and chi_b is transplanted unchanged.
#        The identification ledger counts BOTH as internally solved, so Track
#        B as configured would have measured the robustness of one instrument
#        while being reported as the robustness of the calibration. It now
#        SAYS which it is doing on every cell. Not silently switched to two
#        instruments: that path targets total household wealth W, for which
#        this project has no external target -- inventing one would supply
#        exactly the moment the ledger's V6 says is missing. TRACKB_WTARGET
#        opts in deliberately, with the value recorded in the log.
# R11.21 the provenance audit ran for the first time and its verdict was a
#        FALSE ALARM. The rule demanded that every shadowed name resolve
#        inside src_project; `rouwenhorst` lives in the ROOT package's src/
#        and has no src_project copy, so it could never satisfy the test
#        however correctly it resolved, and the run printed "DO NOT RUN
#        PARITY" on an installation that was in fact safe on both counts.
#        The verdict is now three-way and tests what matters: (a) is the
#        WINNER a project file at all -- a winner outside the repository is
#        disqualifying; (b) for an internal same-name pair, does the SAFE
#        arity polarity win. The two shadows are now classified rather than
#        lumped: solve_household_egm is an INTERNAL collision (fix by
#        renaming) currently resolving the safe way, and rouwenhorst is
#        EXTERNAL contamination from a Dynare 7 MEX on the user's path (fix
#        by path hygiene) which the project .m currently wins. An external
#        shadow that loses today can win tomorrow -- path order decides it and
#        adding a toolchain in the same session reorders it -- so that case
#        now prints its own standing warning.
# R11.20 main_credibility_reversal's first run returned NOTHING USABLE: 9 of
#        12 cells fell under the solver's clearing residual and the
#        front-loading grid printed as all dots. The cause was the sweep
#        design, not the solver. With rbar = 1.96% the map h -> s =
#        rbar/(rbar+h) is violently convex: h = 0.05, an expected program life
#        of 20 years and a mild assumption in words, removes 72% of the
#        program's PV, so the price response scales down with it and vanishes.
#        The sweep is now posed in the PV WEIGHT s (default [1 .9 .8 .7]) with
#        the implied hazard reported beside it; HGRID_EXPLICIT = true restores
#        raw-hazard sweeping. This is itself the answer to the referee's "at
#        least two reversal hazards": at a 2% real rate there is no graded
#        middle ground in h -- holding s >= 0.9 needs an expected life of 459
#        years, i.e. indistinguishable from perfect credibility, while
#        anything a reader would call real reversal risk already halves the
#        program.
# R11.19 the run was on course to DISCARD its own answer. Gate 11 proper is
#        computed only over CERTIFIED cells and no cell certifies, so a full
#        matrix would have reported "not computable" while holding nine
#        perfectly good dP values. contrast_gates now also reports a
#        PROVISIONAL block over every cell that produced a finite dP, split
#        into the two questions that are genuinely different: MAGNITUDE
#        (spread in dP relative to dP -- what Gate 11 asks) and ORDERING
#        (does dP keep its sign across every discretization). The ordering is
#        the weaker and more robust claim because it compares two solves on
#        the SAME grid, where common discretization error differences out.
#        Every line is labelled provisional and uncertified.
#        Also: gate 6 was failing the BEST case. Churn compares the adjuster
#        index arrays across the last two VFI sweeps, so a warm start already
#        at the fixed point converges on sweep 1 with nothing to compare
#        against, reports NaN, and is failed as unmeasured. Now scored
#        not-applicable -- but only when the VFI reached the HARD tolerance
#        and did not soft-accept, so a solve that gave up cannot reach the
#        exemption. vfi_iters is propagated for this.
# R11.18 my gate-4 amendment put its rationale INSIDE the continued
#        struct( ... ) call. MATLAB cannot parse a comment-only line within a
#        `...` continuation -- the error is "Invalid expression" pointing at a
#        column in the middle of the construct, which reads like a delimiter
#        problem. Comment moved above the statement, and
#        paper/check_matlab_blocks.py now checks for this class with six new
#        controls (a trailing comment after `...` is fine; a comment inside
#        [ ] WITHOUT a continuation is fine, since there the newline is a row
#        separator). The project-wide rescan found ONE OTHER instance:
#        src_project/calib_beta_chi.m had its whole header comment between
#        the two lines of its continued signature, so the file could not be
#        parsed at all. It went unnoticed because the shipped benchmark
#        leaves W_targ empty and never calls it -- which means the
#        two-instrument calibration of referee item M3, the one the
#        identification ledger's V6 recommends, would have failed on its
#        first invocation.
# R11.17 the widened FAST matrix runs for hours per cell, so the run is now
#        SURVIVABLE: after every cell (failed ones included -- a failure is a
#        result) the partial CELL array is checkpointed to
#        output/quarantine/cert_<TRACK>_checkpoint.mat, written to a .tmp and
#        renamed so an interrupt during the save cannot leave a truncated
#        file that the next run would trust. Resume is accepted ONLY when a
#        SIGNATURE matches -- frozen parameters, grid extents, curvatures,
#        node lists, track -- because splicing cells from two experiments
#        would make Gate 11 a spread over whatever else changed. RESUME =
#        false forces a clean start.
#        Also: no parallel pool for Track A. Nothing in its call chain has a
#        parfor (the cell loop is serial by protocol, kv_solve_alpha is
#        serial by design, the household solver uses implicit
#        multithreading), so the pool started four idle workers and then
#        announced "IdleTimeout has been reached" mid-cell, which reads as a
#        fault and is not one. PARALLEL now defaults to false.
# R11.16 my churn measure was a size error: aIdx is nx x ne and sIdx is
#        nac x ne, so the elementwise OR between them could not broadcast and
#        every cell died in the household solver. The two arrays are now
#        counted separately and pooled over the combined denominator.
#        TWO PROTOCOL AMENDMENTS, authorised by the project lead, reasons
#        recorded in kv_gate_report's threshold block:
#        (1) the Track A family is WIDENED (bmax x8, kmax x6 from the residual
#            scan) with node counts scaled by F^(1/curvature), so widening is
#            not also coarsening. Parameters stay frozen; the resulting miss
#            against the S_b = 0.30 target is reported per cell as
#            target_drift rather than assumed small. WIDEN = false restores
#            the old matrix; NB_SCALED = false keeps the small node counts.
#        (2) gate 4 lowered 1e-8 -> 1e-6 (= p.tol_vfi, the solver's own hard
#            tolerance), with NEW gate 4.1 failing any equilibrium whose VFI
#            soft-accepted a grid-limited fixed point. 1e-8 was unattainable
#            by construction on a discrete-choice grid; 3e-3 would have been
#            vacuous. The pair asks: did the VFI reach the tolerance it was
#            asked for, by converging rather than by giving up.
# R11.15 TRACK A COULD NOT HAVE PRODUCED GATE 11. Three defects, found in
#        the first two cells of the first real run:
#        (a) kv_solve_alpha assembled its output struct WITHOUT dV, ddist or
#            dist_loose, one hop after kv_solve_bond_given_q had carried them
#            up from kv_stationary_block. kv_gate_report read them as
#            missing, scored gates 4 and 5 NaN, and by its own "unmeasured is
#            never passed" rule failed every cell -- so Gate 11, which
#            accumulates only certified cells, could never be computed. Gate
#            5.1 was worse: defaulting dist_loose to false made ~false = true,
#            so it PASSED on the absence of the flag it checks.
#        (b) gate 6 (adjuster policy churn) was never supplied at all. The
#            household solver recorded pol_stable -- a count of consecutive
#            unchanged sweeps, LARGER when better -- which is not what a
#            "<= 0" gate wants. It now records churn_adj as the FRACTION of
#            adjuster (aIdx, sIdx) pairs that moved on the final sweep, and
#            the driver passes it under the name the gate expects.
#        (c) gates 7-9 are properties of the grid EXTENT, which Track A holds
#            frozen while varying only node counts, so they fail identically
#            in all nine cells. A pre-flight now evaluates them on the frozen
#            benchmark distribution and says so before any solving. NO
#            THRESHOLD AND NO PASS RULE IS CHANGED -- that is a protocol
#            decision, not a code fix.
# R11.14 the ledger's V1 diagnostic came back AGAINST the verdict it was
#        written to support: 3 configs tie on the top-1% target and their
#        untargeted moments agree to within the tie band, so the superstar
#        block measures as locally identified and the a-priori count
#        overstates the slack there. V1 is now posed as a question and
#        answered by the measurement. Also fixed: section C was reporting
#        KVJ's TARGET (kvj_logel = -0.75/0.73) in the MODEL column, so that
#        row would have compared a number against itself once the data slot
#        was filled; the model side is now -zeta*b/(b+bbar) at the calibrated
#        position, which is what the KVJ coefficient is comparable to.
#        New: paper/check_output_staleness.py.
# R11.13 D10 PARITY ESTABLISHED (A/B/C all PASS: 40/32/32 fields exact).
#        Three changes it exposed: (1) leg 2 of main_parity_d10_calibration
#        runs the production script, which SAVES to the same output files
#        the paper reads, so a FAST parity run silently replaced the
#        benchmark two-asset economy with a coarse-grid one; the driver now
#        backs those files up and restores them on both the success and the
#        error path. (2) main_identification_ledger now reports the grid
#        sizes of the two-asset .mat and shouts when they are below
#        production, because nothing else in the file says it is FAST.
#        (3) wealth_concentration_fit emits an identification diagnostic and
#        accepts a second concentration target; with TOP10_TARGET = NaN the
#        selection is bit-identical to before, so no downstream result moves.
# R11.12 new driver main_identification_ledger (referee R12, Major 2 items
#        1-3). It is READ-ONLY over stored calibrations and solves nothing,
#        so it changes no numerical result: no existing driver's interface
#        moved and the D10/D11 parity legs are unaffected. The revision is
#        bumped only so a pasted ledger log identifies itself.
# R11.11 D10 comparison A PASSED (40 exact, 0 FAIL): the calibration refactor
#        is behaviour-preserving. B/C failed because leg 3 was handed the
#        script's OUTPUTS as inputs -- p_base = SCRIPT.p and q_ref =
#        SCRIPT.eq_q -- so the callable started AT the solution while the
#        script started from RCAL.beta_star. Leg 3 now recovers beta0, q_ref
#        and chi_ref from the script's own workspace.
# R11.10 syntax fix in kv_code_version (adjacent string literals across a
#        continuation are not concatenated by MATLAB; they need [ ]), plus a
#        static check for that class in paper/check_matlab_blocks.py with
#        seven new controls. Whole project rescanned: no other instance.
# R11.9  D11: dlnP0 and kappa_legacy were dropped when the derived statistics
#        were consolidated in R11.7, so the baseline carried them and the
#        current legs did not -- two FAILs on a run where every economic field
#        was bit-identical. Restored inside derived(). kv_code_version now
#        reports when CODE_VERSION.txt comes from a different folder than the
#        running driver.
# R11.8  D10: the baseline tree's output/ was EMPTY while the current tree's
#        was full, so main_twoasset_ownership_kv read different cached inputs
#        in the two legs and they solved DIFFERENT ECONOMIES (p_in 0.006 vs
#        0.000 -> a different income grid). The capture now mirrors and hashes
#        the four cached inputs; the parity driver gates on them. Also: the two
#        captures extracted different eq_* field lists; nested structs are
#        flattened instead of compared whole; fields outside the callable's
#        contract are excluded from B and C.
# R11.7  D11 harness fixes: pgc/calinfo are provenance INPUTS and are no longer
#        compared as outputs (they were manufacturing two FAILs from a run in
#        which all 47 economic fields were bit-identical); the residual print
#        no longer recycles fprintf over a vector; one derived-statistics
#        function for all legs; cross-instrument overshoot renamed away from
#        "front_loading"; convergence and nominal-neutrality gates added.
# R11.6  the D11 transition legs now use the LEGACY setup (calibrated beta via
#        calibrate_beta, climate_version, D0, Gg_nom, FAST asset grid). Passing
#        the default beta left no root in the solver's [0.5, 1.3] bracket and
#        the baseline steady state did not exist. Same defect fixed in
#        kv_kappa_legacy. The parity driver now reuses the CAPTURED pgc.
# R11.5  the baseline/current calibration script is invoked through a local
#        function, so its `clearvars -except ...` cannot delete the caller's
#        workspace. Fixes load(stash) after `stash` was itself cleared, and
#        the same latent bug in main_parity_d10_calibration (pg, t2).
# R11.4  SHA-256 is computed in pure MATLAB (kv_sha256), with its own NIST
#        self-test. R2025b has no JVM, so java.security.MessageDigest was
#        unavailable and the baseline could not be verified at all.
# R11.3  baseline capture is git-free: baseline_bf0a4e8/ ships with the project
#        and is verified by SHA-256 manifest. main_baseline_capture takes no
#        required variables. Version banners added.
# R11.2  three-way D10/D11 parity against bf0a4e8; C3 withdrawn as infeasible;
#        consolidation surcharge; double-widening fix (kv_ensure_widened).
# R11.1  figure terminology at source; mangled-macro check; p.29 repair.
