R11.12
# One line, above, is the pipeline revision. Everything below is a comment.
#
# Bump this whenever a driver's INTERFACE or required inputs change, so that a
# pasted console log identifies which revision produced it. This exists because
# a stale main_baseline_capture.m -- one that still required a git worktree and
# a BASELINE_OUT variable -- was run against a set of instructions written for
# the rewritten one, and the only symptom was an assertion about a variable
# that the current file does not mention. The revision is now printed by every
# driver in the parity sequence, next to that file's own modification date.
#
# R11.12 new driver main_identification_ledger (referee R12, Major 2 items
#        1-3). It is READ-ONLY over stored calibrations and solves nothing,
#        so it changes no numerical result: no existing driver's interface
#        moved and the D10/D11 parity legs are unaffected. The revision is
#        bumped only so a pasted ledger log identifies itself.
# R11.11 D10 comparison A PASSED (40 exact, 0 FAIL): the calibration refactor
#        is behaviour-preserving. B/C failed because leg 3 was handed the
#        script's OUTPUTS as inputs -- p_base = SCRIPT.p and q_ref =
#        SCRIPT.eq_q -- so the callable started AT the solution while the
#        script started from RCAL.beta_star. Leg 3 now recovers beta0, q_ref
#        and chi_ref from the script's own workspace.
# R11.10 syntax fix in kv_code_version (adjacent string literals across a
#        continuation are not concatenated by MATLAB; they need [ ]), plus a
#        static check for that class in paper/check_matlab_blocks.py with
#        seven new controls. Whole project rescanned: no other instance.
# R11.9  D11: dlnP0 and kappa_legacy were dropped when the derived statistics
#        were consolidated in R11.7, so the baseline carried them and the
#        current legs did not -- two FAILs on a run where every economic field
#        was bit-identical. Restored inside derived(). kv_code_version now
#        reports when CODE_VERSION.txt comes from a different folder than the
#        running driver.
# R11.8  D10: the baseline tree's output/ was EMPTY while the current tree's
#        was full, so main_twoasset_ownership_kv read different cached inputs
#        in the two legs and they solved DIFFERENT ECONOMIES (p_in 0.006 vs
#        0.000 -> a different income grid). The capture now mirrors and hashes
#        the four cached inputs; the parity driver gates on them. Also: the two
#        captures extracted different eq_* field lists; nested structs are
#        flattened instead of compared whole; fields outside the callable's
#        contract are excluded from B and C.
# R11.7  D11 harness fixes: pgc/calinfo are provenance INPUTS and are no longer
#        compared as outputs (they were manufacturing two FAILs from a run in
#        which all 47 economic fields were bit-identical); the residual print
#        no longer recycles fprintf over a vector; one derived-statistics
#        function for all legs; cross-instrument overshoot renamed away from
#        "front_loading"; convergence and nominal-neutrality gates added.
# R11.6  the D11 transition legs now use the LEGACY setup (calibrated beta via
#        calibrate_beta, climate_version, D0, Gg_nom, FAST asset grid). Passing
#        the default beta left no root in the solver's [0.5, 1.3] bracket and
#        the baseline steady state did not exist. Same defect fixed in
#        kv_kappa_legacy. The parity driver now reuses the CAPTURED pgc.
# R11.5  the baseline/current calibration script is invoked through a local
#        function, so its `clearvars -except ...` cannot delete the caller's
#        workspace. Fixes load(stash) after `stash` was itself cleared, and
#        the same latent bug in main_parity_d10_calibration (pg, t2).
# R11.4  SHA-256 is computed in pure MATLAB (kv_sha256), with its own NIST
#        self-test. R2025b has no JVM, so java.security.MessageDigest was
#        unavailable and the baseline could not be verified at all.
# R11.3  baseline capture is git-free: baseline_bf0a4e8/ ships with the project
#        and is verified by SHA-256 manifest. main_baseline_capture takes no
#        required variables. Version banners added.
# R11.2  three-way D10/D11 parity against bf0a4e8; C3 withdrawn as infeasible;
#        consolidation surcharge; double-widening fix (kv_ensure_widened).
# R11.1  figure terminology at source; mangled-macro check; p.29 repair.
